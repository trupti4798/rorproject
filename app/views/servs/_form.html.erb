







<%= form_with(model: serv) do |form| %>
  <% if serv.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(serv.errors.count, "error") %> prohibited this serv from being saved:</h2>

      <ul>
        <% serv.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :info %>
    <%= form.text_field :info  %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
